@model IEnumerable<PharmacuticalE_Commerce.ViewModels.AttendancesViewModel>

@{
    ViewBag.PageTitle = "Show Attendances";
    ViewBag.CardTitle = "Attendance Records";
    Layout = "~/Views/Shared/_StaffManagementLayout.cshtml";
}

<table class="table">
    <thead>
        <tr>
            <th>Employee Name</th>
            <th>Branch</th>
            <th></th>
            <th></th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.FirstName @item.LastName</td>
                <td>@item.BranchAddress</td>
                <td>
                    <a asp-action="Details" asp-route-id="@item.RecordId" class="btn btn-info">Details</a>
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.RecordId" class="btn btn-warning">Edit</a>
                </td>
                <td>
                    <a asp-action="Delete" asp-route-id="@item.RecordId" class="btn btn-danger">Edit</a>
                </td>
            </tr>
        }
    </tbody>
</table>
