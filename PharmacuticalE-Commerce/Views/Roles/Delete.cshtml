@model PharmacuticalE_Commerce.Models.Role

@{
    ViewBag.PageTitle = "Employees Roles";
    ViewBag.CardTitle = "Delete";
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_StaffManagementLayout.cshtml";
}

<h3>@($"Are you sure you want to delete {Model.Title} Role ?")</h3>
<div>
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Title)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Description)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Description)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="RoleId" />
        <input type="submit" value="Delete" class="btn btn-danger" /> &nbsp;&nbsp;&nbsp;
        <a asp-action="Index" class="btn btn-secondary">Back to List</a>
    </form>
</div>
